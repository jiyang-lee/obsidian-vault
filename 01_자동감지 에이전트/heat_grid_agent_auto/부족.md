
# package만으로 부족한 것

|부족한 것|왜 문제인지|
|---|---|
|`risk_model_best.joblib` 없음|새 window에 risk 예측을 새로 못 함|
|`leadtime_model_best.joblib` 없음|새 window에 leadtime 예측을 새로 못 함|
|`priority_engine_best_metadata.json` 없음|Current-Best priority engine 원본 근거가 없음|
|raw -> canonical `trainable_windows.csv` 생성 코드 없음|새 raw를 모델 입력 window로 만드는 완전 파이프라인이 없음|
|코드가 `C:\Project3\HeatGrid_Agent\best`를 참조|현재 package 밖의 best 프로젝트 결과에 의존|
|M1 gate 재실행 코드가 `C:\Project3\3rd_project_for_ML-main`을 참조|joblib은 package에 있지만, 코드상 metadata/model copy는 외부 프로젝트를 기대함|
|현재 raw 데이터는 zip으로만 있음|코드가 기대하는 건 압축 해제된 `manufacturer 1/operational_data/...` 구조|

# 근거 부족 및 추가 확인 필요한 결정


| 검토 순서 | 카테고리                              | 항목                                              | 현재 상태                                        | 약한 점                                     | 해결책 / 다음 액션                                                                                                             |
| ----- | --------------------------------- | ----------------------------------------------- | -------------------------------------------- | ---------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| 1     | 데이터 coverage / Current-Best 연결    | missing row 26개                                 | canonical window 1252개 중 priority/card 1226개 | 빠진 26개가 모두 `pre_fault`; 영향 설명 필요         | Current-Best 원본 `best` 프로젝트에서 어디서 빠졌는지 추적. `risk_scores`, `leadtime_scores`, `priority_scores` 순서로 key coverage 확인      |
| 2     | 적용 범위 / 모델 scope                  | M1-only scope                                   | manufacturer 1만 대상으로 검증                      | M2 확장 불가, M2 calibration/validation 제외   | 발표/문서에서 M1 전용 모델로 명확히 제한. M2는 별도 calibration 필요하다고 명시. M2 적용 시 M2-only split/threshold 재학습                              |
| 3     | Anomaly 모델                        | anomaly 기준 `IF >= 0.90 AND Mahalanobis >= 1.00` | 성능 리포트는 있음                                   | 왜 0.90/1.00 조합인지 더 자세한 선택 근거 약함          | IF threshold와 Mahalanobis threshold를 grid search. 예: IF `0.8~1.1`, Mahalanobis `0.8~1.2` 조합별 FPR/recall 비교              |
| 4     | Anomaly 모델                        | criticality threshold `5`                       | 지속 이상이면 event label                          | 왜 5인지 운영/실험 근거 부족                        | criticality `1~10` sweep. “몇 window 연속 이상이어야 알람으로 볼지”를 FPR/recall/lead time 기준으로 비교                                     |
| 5     | M1 specialist 모델                  | [[Gate]] threshold `0.5 / 0.6`                  | RF gates 0.5, pre-event 0.6                  | threshold 선택 실험/근거가 충분히 드러나지 않음          | gate별 threshold sweep 수행. `0.3~0.8` 구간에서 precision/recall/FPR 비교 후 threshold 재선정                                        |
| 6     | M1 specialist 모델                  | [[M1 Specialist Priority 점수]]`0.55/0.30/0.15`   | `pre_event / leadtime / group_weight` 조합     | 왜 이 비율인지 별도 ablation 근거 부족               | `pre_event`, `leadtime`, `group_weight` 각각 제거한 ablation 수행. 예: `0.7/0.3/0`, `0.6/0.4/0`, `1.0/0/0`, `0.55/0.30/0.15` 비교 |
| 7     | M1 specialist 모델                  | [[fault_group_weight]]                          | pump, valve, leakage 등에 하드코딩 가중치 있음          | 각 가중치 산정 근거가 package 안에 없음               | 고장군별 빈도/심각도/탐지성 기준표 작성. 근거 없으면 일단 모든 group weight를 동일값으로 둔 baseline과 비교                                                 |
| 8     | Hybrid priority / 최종 점수 결합        | [[0.65와 0.35]] hybrid weight                    | 문서/테스트에 공식처럼 박혀 있음                           | 직접 sweep해보니 `0.9 / 0.1`이 holdout에서 더 좋았음 | weight sweep 결과를 근거로 `0.9/0.1`을 후보안으로 제안. 단, 팀 결정 전 `0.8/0.2`, `0.9/0.1`, `1.0/0.0`을 top-K, precision, FPR 기준으로 함께 비교   |
| 9     | Priority card / level calibration | `FPR <= 0.20` [[level]] calibration             | validation threshold 선택 기준                   | 20% 허용치가 운영적으로 왜 적정한지 근거 필요              | FPR cap을 `0.05`, `0.10`, `0.15`, `0.20`으로 바꿔 level threshold 재계산. 운영 리소스 기준으로 허용 가능한 오탐률 선택                             |

# 가장 위험한 3가지

1. 0.65/0.35가 최적처럼 보이지만 실제 sweep에서는 0.9/0.1이 더 좋음
2. 새 raw 데이터 재예측에 필요한 risk/leadtime/current-best 본체가 없음
3. canonical window에서 빠진 26개가 모두 pre_fault라 평가/커버리지 해석에 영향 가능

